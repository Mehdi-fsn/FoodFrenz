import 'package:flutter/material.dart';

class PaymentSettingsPage extends StatelessWidget {
  const PaymentSettingsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PaymentSettingsPage'),
    );
  }
}
